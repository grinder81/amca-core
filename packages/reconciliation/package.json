{
  "name": "@amca/reconciliation",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic advisory reconciliation for admitted AMCA receipts and observations; not proof, release, broker, adapter, or service authority.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run --root ../.. packages/reconciliation/src"
  },
  "dependencies": {
    "@amca/contracts": "workspace:*",
    "@amca/protocol": "workspace:*"
  }
}
